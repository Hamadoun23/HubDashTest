Django==6.0.8
inertia-django==1.2.0

# Postgres (Docker — voir docker-compose.yml). psycopg[binary] : pas de
# compilation C requise sous Windows, même choix que backend/financerh.
dj-database-url==3.1.0
psycopg[binary]==3.2.12
bcrypt==5.0.0
python-dotenv==1.2.2

# Exports
openpyxl==3.1.5
python-docx==1.2.0
xhtml2pdf==0.2.17

# Compte unique de GDA Hub : verification du jeton RS256 signe par identity.
# cryptography est indispensable — sans lui, PyJWT leve « Algorithm not
# supported » a la premiere requete. Voir core/hub.py.
PyJWT>=2.8
cryptography>=42.0
